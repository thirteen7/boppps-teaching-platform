# docker-compose wires the app to the bundled MySQL service `db`.
# Override only if you run the Flask image without compose.

SQLALCHEMY_DATABASE_URI=mysql+pymysql://koppps:rootroot@db:3306/koppps?charset=utf8mb4
JWT_SECRET_KEY=super-secret-key
